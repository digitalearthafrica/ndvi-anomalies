name : ndvi-anomalies Test

on:
  pull_request:
    branches:
      - main
    paths:
      - 'docker/**'
      - '.github/workflows/ndvi-anomalies-*'
      - 'production/ndvi_tools/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

#     - name: Check files using the black formatter
#       uses: lgeiger/black-action@master
#       with:
#         args: "production/ndvi_tools --check"

#     - name: Annotate diff changes using reviewdog
#       if: ${{ failure() }}
#       uses: reviewdog/action-suggester@v1
#       with:
#         tool_name: blackfmt

    - name: Build ndvi-anomalies image
      timeout-minutes: 20
      shell: bash
      run: |
        docker build . --tag digitalearthafrica/ndvi-anomalies
